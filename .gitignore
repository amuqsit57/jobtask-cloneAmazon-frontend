node_modules/
.DS_Store

# Every env file stays local. Real values live in the host dashboard.
# .env.example is the only one committed, and it holds placeholders.
.env
.env.*
!.env.example

# --- Next.js ---
/.next/
/out/
/build
.vercel
*.tsbuildinfo
next-env.d.ts
/coverage
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# .agent-logs/ ships with this repo as part of the 8x submission.
# Never ignore it. This negation is deliberate and must stay last so it wins
# over anything a scaffolder appends above.
!.agent-logs/
!.agent-logs/**
